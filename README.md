# AVI Design Studio — client presentation

Password-protected static page. The deck HTML is encrypted (AES-256-GCM,
PBKDF2-SHA256) and decrypted in the browser after the access code is entered.

Source and build script live in the private repo `avi-pitch-deck`.
